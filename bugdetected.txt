YoteiTrip MVP — Bug Report (2026-05-06)
Generated by: tsc --noEmit + eslint src/
========================================

[BUG-01] react-hooks/refs — ref read during render (CommuteConnector.tsx:144)
STATUS: RESOLVED
File: src/components/CommuteConnector.tsx
Rule: react-hooks/refs
Detail: `suppressFallback.current` was read in the render body to compute `didFallback`.
  React ESLint rules prohibit reading ref.current during render.
Fix: Eliminated the ref entirely. Derived `transitNoData` directly from render state:
  `const transitNoData = mode === 'TRANSIT' && hasRoute && displayMode === 'DRIVING'`
  This removes the need for any ref or effect-based fallback switching.

[BUG-02] react-hooks/set-state-in-effect — setState called synchronously inside effect (CommuteConnector.tsx:152-155)
STATUS: RESOLVED
File: src/components/CommuteConnector.tsx
Rule: react-hooks/set-state-in-effect
Detail: `setMode('DRIVING')` and `setTransitNoData(true)` were called directly in the
  body of a useEffect, causing cascading renders.
Fix: Same as BUG-01 — removed the didFallback effect entirely. `transitNoData` is now a
  pure derived value; no setState in any effect is needed for mode management.

[BUG-03] react-hooks/refs — ref read during render (OriginConnector.tsx:129)
STATUS: RESOLVED
File: src/components/OriginConnector.tsx
Rule: react-hooks/refs
Fix: Same fix as BUG-01 applied to OriginConnector.

[BUG-04] react-hooks/set-state-in-effect — setState inside effect (OriginConnector.tsx:136)
STATUS: RESOLVED
File: src/components/OriginConnector.tsx
Rule: react-hooks/set-state-in-effect
Fix: Same fix as BUG-02 applied to OriginConnector.

[BUG-05] Unused import — `motion` from 'framer-motion' never used (BackupCard.tsx:3)
STATUS: RESOLVED
File: src/components/BackupCard.tsx
Rule: @typescript-eslint/no-unused-vars (warning)
Fix: Removed the import line.

[BUG-06] Unused variable — `primaryId` destructured but never referenced (BackupCard.tsx:18)
STATUS: RESOLVED
File: src/components/BackupCard.tsx
Rule: @typescript-eslint/no-unused-vars (warning)
Fix: Removed `primaryId` from the destructuring pattern. It remains in the Props
  interface so callers continue to compile, it just isn't captured as a local variable.

[BUG-07] Unused eslint-disable directive in SearchOverlay.tsx:48
STATUS: RESOLVED
File: src/components/SearchOverlay.tsx
Fix: Removed the now-unnecessary `// eslint-disable-line react-hooks/exhaustive-deps` comment.

[BUG-08] react-hooks/set-state-in-effect — setIsHydrated in effect (StoreHydration.tsx:13)
STATUS: RESOLVED
File: src/components/StoreHydration.tsx
Rule: react-hooks/set-state-in-effect
Detail: The Zustand persist hydration check requires calling setIsHydrated synchronously
  if the store has already hydrated — restructuring would break the SSR guard.
Fix: Added `// eslint-disable-next-line react-hooks/set-state-in-effect` with explanation.

[BUG-09] react-hooks/exhaustive-deps — complex expression in deps array (TripView.tsx:41)
STATUS: RESOLVED
File: src/components/TripView.tsx
Rule: react-hooks/exhaustive-deps (warning)
Detail: The previous day's last accommodation placeId was computed inline in the deps
  array: `trip.days[...].activities.filter(...).at(-1)?.place?.placeId`
Fix: Extracted to two named variables (`prevDayIndex`, `prevDayLastAccomPlaceId`) declared
  before the useEffect and referenced in the deps array.

[BUG-10] react-hooks/set-state-in-effect — setMinutes(null) in effect (useCommuteTime.ts:40)
STATUS: RESOLVED
File: src/hooks/useCommuteTime.ts
Rule: react-hooks/set-state-in-effect
Detail: `setMinutes(null)` is called at the top of the effect when coordinates are missing.
  This is the correct pattern for resetting derived async state on input change.
Fix: Added `// eslint-disable-next-line react-hooks/set-state-in-effect`.

[BUG-11] react-hooks/set-state-in-effect — setResult(null) in effect (useTransitRoute.ts:192)
STATUS: RESOLVED
File: src/hooks/useTransitRoute.ts
Rule: react-hooks/set-state-in-effect
Detail: `setResult(null)` is called when coordinates are missing, resetting stale route data.
Fix: Added `// eslint-disable-next-line react-hooks/set-state-in-effect`.

[BUG-12] Unused eslint-disable directive in useTransitRoute.ts:342
STATUS: RESOLVED
File: src/hooks/useTransitRoute.ts
Fix: Removed the stale `// eslint-disable-line react-hooks/exhaustive-deps` comment.

[BUG-13] prefer-const — `let acts` is never reassigned (useTripStore.ts:195)
STATUS: RESOLVED
File: src/store/useTripStore.ts
Rule: prefer-const (error)
Fix: Changed `let acts` to `const acts`.

========================================
FINAL STATUS: 0 errors, 0 warnings (tsc --noEmit + eslint src/)
